import { graphql, Link } from "gatsby"
import React from "react"

import Layout from "../../stylingreact/layout"

export default function StylingReactIndexPage(props) {
  return (
    <Layout>
      <main className="home-main">asdf</main>
    </Layout>
  )
}
